// Generated by CoffeeScript 2.5.1
module.exports = function(argList, wishTypes, fnName, _this) {
  var a, arg, b, i, j, len, results, typeOf, wishType;
  typeOf = function(obj) {
    return Object.prototype.toString.call(obj).replace(/\[object|\]/g, "").toLowerCase().trim();
  };
  results = [];
  for (i = j = 0, len = wishTypes.length; j < len; i = ++j) {
    wishType = wishTypes[i];
    arg = argList[i];
    if (typeOf(wishType) === "array") {
      if (!wishType.some(function(choise) {
        return choise === typeOf(arg); //有一个选项满足即可
      })) {
        throw new TypeError(`{${fnName != null ? fnName : "fn?"}} \#${!_this ? i + 1 : "0"} need [${wishType.join(" or ")}] not [${typeOf(arg)}]`);
      } else {
        results.push(void 0);
      }
    } else if (typeOf(wishType) === "object") {

    } else {
      if (typeOf(arg) !== wishType.replace("?", "")) {
        if ((a = !(wishType.includes("?"))) || (b = wishType.includes("?") && (typeOf(arg) !== "undefined"))) {
          throw new TypeError(`{${fnName != null ? fnName : "fn?"}} \#${!_this ? i + 1 : "0"} need [${wishType}] not [${typeOf(arg)}]`);
        } else {
          results.push(void 0);
        }
      } else {
        results.push(void 0);
      }
    }
  }
  return results;
};
